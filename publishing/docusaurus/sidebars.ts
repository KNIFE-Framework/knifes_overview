// sidebars.ts – shared sidebars for multi-docs setup (sk, en)
import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

/**
 * IMPORTANT
 * - Keys in this object must match the plugin `id` defined in docusaurus.config.ts.
 * - For each plugin, `id` references are LOCAL to that plugin's docs folder.
 *   e.g., in the `sk` plugin, the doc id 'KNIFE_HOME' resolves to docs/sk/index.md (with id: KNIFE_HOME).
 */

const sidebars: SidebarsConfig = {
  // Slovak docs (plugin id: 'sk')
  sk: [
    // Landing page for /sk (requires docs/sk/index.md with id: KNIFE_HOME, slug: "/")
    { type: 'doc', id: 'KNIFE_HOME', label: '🏠 Domov' },

    // 7Ds (autogenerated from docs/sk/7Ds)
    {
      type: 'category',
      label: '7Ds',
      collapsed: false,
      items: [{ type: 'autogenerated', dirName: '7Ds' }],
    },

    // KNIFES (autogenerated from docs/sk/knifes)
    {
      type: 'category',
      label: 'KNIFES',
      collapsed: false,
      items: [{ type: 'autogenerated', dirName: 'knifes' }],
    },
  ],

  // English docs (plugin id: 'en')
  // Temporarily disable sidebar items for EN to avoid empty-category errors.
  // When you add docs/en content, re-enable categories or add a Home doc.
  en: [],
};

export default sidebars;